/* 
 * ROS interface using actionlib to do calls to the regression library
 * Conversion from point cloud types, mainly msgs
 * Instances of the regressor according to parameters.
 *
 */

#include <gp_regression/gp_regressor.h>

int main( int argc, char** argv )
{
        return 0;
}